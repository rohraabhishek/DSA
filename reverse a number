#include <iostream>

using namespace std;

int main()
{
    int num;
    cin>>num;
    int lastdigit;
    int reverse = 0;
    
    while(num>0){
        lastdigit = num%10;
        reverse = reverse*10 + lastdigit;
        num = num/10;
    }
    cout<<reverse<<endl;
    
    return0;
}
